
build:
  name: "{{ .Name }}_{{ .Os }}_{{ .Timestamp }}"
  snapshot: true
  platforms:
    - amd64
  archs:
    - windows
checksum:
  name: fun
  algorithm: sha256
archive:
  path: "../archivesss"
  name: "{{ .Name }}_{{ .Os }}"
